# 12. 경진대회 제출 요건 대응 — 산출물 제출 요구사항 ↔ 이 프로젝트

> 근거 문서: 「(SDU COSS) 공공데이터 활용 바이브코딩 경진대회 산출물 제출 요구사항 안내」(레포 루트 PDF).
> 이 문서는 요구사항 전문을 프로젝트 결정에 매핑한 **제출 관점의 단일 진실**이다.
> 심사 방식: 제출된 **배포 URL을 전시 플랫폼에 등록해 심사위원이 직접 접속·확인**한다.
> 시상: 최우수 1팀 · 우수 2팀 · 장려 2팀 (총 5팀).

## 1. 필수 제출물 5종과 이 프로젝트의 대응

| # | 요구 제출물 | 이 프로젝트의 대응 | 상태 |
|---|---|---|---|
| 1 | 깃허브 저장소 URL (**Public 필수**) | `https://github.com/yutakdv/sangseng-navigator` | ⚠ **현재 Private + 커밋 0개** — Phase 0에서 초기 커밋, 제출 전 Public 전환 (§4 절차) |
| 2 | 배포 URL (접속 즉시 실행, https) | Vercel `https://<project>.vercel.app` (요구사항 표의 공식 지원 방법) | Phase 1 배포 리허설에서 확보 (09 문서) |
| 3 | 산출물 정보 (팀명·산출물명·한 줄 소개·활용 데이터셋명) | §3 제출 문안 확정본 | 문안 완료, 제출 양식 기재만 남음 |
| 4 | README.md (소개·활용 데이터·주요 기능·실행 방법) | 루트 README를 심사 요건 구조로 개편 완료 | 배포 URL 기입 + `data/processed/` 커밋(Phase 2) 후 현재형 문구 최종 확인 |
| 5 | (선택) 대표 스크린샷 1장 — 전시 썸네일 | 홈(허브) 화면 캡처 — 목업 image-1 방향의 실 구현 화면. Phase 6 리허설 중 1920×1080 캡처 | Phase 6 |

## 2. 배포 요건 ↔ 아키텍처 정합 점검

이미 계획이 요구사항과 정확히 맞아떨어지는 항목 (발표 소재로도 활용):

| 요구사항 | 현 계획의 대응 | 비고 |
|---|---|---|
| 정적 데이터 방식 권장 ("필요한 데이터를 미리 CSV/JSON으로 저장소에 포함") | 파이프라인이 `data/processed/*.json` 사전 계산 → 커밋 → BE 서빙 + FE mock 동일 원천 (02 문서) | **권장 해결 방법을 그대로 구현한 구조** — 심사 Q&A에서 명시할 것 |
| API 인증키를 소스코드에 노출 금지 (Public 저장소 경고) | 공공데이터·지오코딩 키는 파이프라인(로컬) 전용, Lambda에는 LLM 키만 SAM NoEcho 파라미터 (02·04 문서) | §4의 커밋 이력 검사 필수 |
| 로그인 없이 접속 가능 | 인증 없음 (담당자 화면 = 공개 데모, 02 문서) | Vercel Password Protection **끄기** 확인 |
| CORS/Mixed Content 차단 주의 | FE→API Gateway는 https + CORS 허용 구성 (09 문서) | 심사 기간에는 CORS `*` 유지 판단 (09 §5) |
| 배포 중단 없이 심사 기간 유지, sleep 주의 | Vercel(정적·sleep 없음) + Lambda(상시, 콜드스타트만 존재) | §5 심사 기간 운영 모드 |

## 3. 산출물 정보 — 제출 양식 문안 (확정본)

| 항목 | 제출 문안 |
|---|---|
| 팀명 | V.I.B.E |
| 산출물명 | 상생 나침반 (Sangseng Navigator) |
| 한 줄 소개 | 강원랜드 담당자의 분기별 지역상생 의사결정을 지원하는 AI 정책 나침반 — 하이원포인트 소비 쏠림 진단부터 가맹점 확충·페이백 정책 제안, 담당자 승인, 방문객 추천 위젯 반영까지 |
| 활용한 강원랜드 데이터셋명 | (주)강원랜드_하이원포인트 사용현황 · (주)강원랜드_하이원포인트 가맹점 상세정보 · (주)강원랜드_일자별 카지노 입장객 현황 |
| (병기) 외부 공공데이터 | 소상공인시장진흥공단_상가(상권)정보 · 기상청_단기예보 조회서비스(초단기실황) · 국세청_사업자현황(사업존속연수별) |
| 저장소 URL | https://github.com/yutakdv/sangseng-navigator |
| 배포 URL | `https://<project>.vercel.app` — Phase 1에서 확보 후 기입 |

## 4. Public 저장소 전환 — 절차와 안전 점검

전환 시점: **늦어도 제출 직전**(전환 전까지는 Private로 개발해도 무방). 전환 전 아래를 모두 통과할 것.

1. **시크릿 스캔**: 커밋 이력 전체에 키가 없는지 확인 — `git log -p | grep -iE "sk-|api[_-]?key\s*=" `
   수준의 수동 확인 + `.env`가 이력에 한 번도 커밋된 적 없는지 (`git log --all --oneline -- .env`).
   발견 시: 해당 키 즉시 재발급(로테이션)이 원칙 (이력 세탁보다 빠르고 확실 — 04 문서 §9).
2. **개인정보 파일 확인**: `기획서_V.I.B.E.pdf`(대표자 휴대전화 명기)와 `MVP_상생나침반_개정판.pdf`
   (텍스트 스트림에서 전화번호 패턴 검출 — 커밋 전 수동 확인)는 **커밋하지 않는다** (.gitignore 등재됨).
   커밋할 기획 자료가 필요하면 연락처를 지운 사본을 쓴다. 경진대회 안내 PDF·목업 이미지·공공데이터
   CSV는 공개 무방. 초기 커밋 전 `git status`의 untracked 목록을 전수 분류해 이 목록 밖 파일이
   섞이지 않았는지 확인한다.
3. **데이터 라이선스**: `data/raw/` 원본 CSV와 data.go.kr **오픈 API 원응답 캐시**는 공공데이터포털
   제공분이라 공개 무방. 단 **Kakao 지오코딩 캐시(`geocode.json`)는 공공데이터가 아니고** 카카오
   정책상 원응답 저장·재배포 제약이 있어 커밋하지 않는다 (.gitignore 등재, 06 문서 P3) — 재현성은
   파이프라인 산출물 `merchants.json`(주소·좌표 결과값만)이 담보한다.
4. (선택) **LICENSE**: 대회 요구사항은 아니나(안내문 전문 확인) Public 레포에 라이선스가 없으면
   법적으로 all rights reserved다. 팀 합의 후 MIT LICENSE 추가 권장 (저작자 표기: 팀 V.I.B.E).
5. 전환: GitHub → Settings → General → Danger Zone → Change visibility → Public.
6. 전환 후: GitHub Pages는 쓰지 않으므로 추가 설정 없음. Vercel 연동은 가시성 변경과 무관하게 유지된다.

## 5. 심사 기간 운영 모드 (배포 URL이 "무인 전시"된다는 전제)

심사위원은 데모 시나리오 안내 없이, 임의 시점에, 임의 기기(휴대폰 포함)로 접속한다. 이에 따른 리스크와 대응:

| 리스크 | 내용 | 대응 (구현 시점) |
|---|---|---|
| Lambda 콜드스타트 | 첫 접속 API 응답 1~3초 지연 → "데이터 안 나옴"으로 오인 가능 | ① FE 로딩 스켈레톤 필수(F9) ② 여유 시 EventBridge `rate(5 minutes)` → `/api/health` 핑 워밍 룰을 SAM에 추가 (월 ~8.6천 호출, 프리티어 내 $0) (Phase 6) |
| 데모 상태 오염 | 공개 URL이므로 심사위원이 승인/반려 클릭 → seed 서사(고정해 둔 "Score 2위 → 1순위 제안" pending 카드)가 소실될 수 있음 | `seed_demo.py --reset`을 (a) 발표·심사 시작 직전 수동 실행, (b) 여유 시 EventBridge 스케줄(매시)로 자동 리셋 (B4 시드 스크립트 확장, Phase 6) |
| LLM 호출 남용/비용 | 공개 URL에서 카드 생성·시뮬레이션 버튼 반복 클릭 가능 | ① generate는 pending 중복 가드가 이미 사실상 rate limit (05 §8) ② gpt-4o-mini 고정 + Billing $1 알림 ③ 여유 시 Lambda `ReservedConcurrentExecutions: 5` (Phase 6, 선택) |
| FE만 살고 BE가 죽는 경우 | API 장애 시 화면 공백 → 심사 탈락급 리스크 | `lib/api.ts` 구조상 `NEXT_PUBLIC_API_BASE` 미설정 빌드는 mock(실데이터 사본)으로 동작 — **비상시 env 제거 후 Redeploy가 최후 폴백** (mock도 강원랜드 실데이터 산출물이므로 "데이터 정상 표시" 요건 충족) |
| 마감 후 커밋 무효 | "제출 마감 이후 커밋은 심사에 반영되지 않을 수 있음" | 마감 전 최종 커밋에 `git tag submission-final` + 이후 main 동결 (핫픽스는 심사 영향 없음을 전제로만) |
| 배포가 일정 후반 1회로 몰림 | 개발 중 AWS 미배포 결정(09 §4)으로 첫 실배포가 마감 직전 | ① `sam validate`·`build`는 사전 통과(리스크 절반 해소) ② IAM 정책은 배포 **전날까지** 부착 ③ 최종 배포에 반나절 버퍼 배정 ④ 실패 시 localhost 데모 백업(11 문서 §4) |
| 휴대폰 접속 | 체크리스트에 "휴대폰에서도 열리는지" 명시 | 13 문서 §8 반응형 최소선 + Phase 6 리허설에 모바일 실기기 1회 포함 (기존 계획 유지·강화) |

## 6. 제출 전 최종 체크리스트 (안내문 원문 + 프로젝트 특화)

안내문 체크리스트 8항 (하나라도 미통과 시 전시·심사 불가):

- [ ] 저장소 Public 설정 (§4 안전 점검 선행)
- [ ] README.md에 프로젝트 소개·활용 데이터·주요 기능·실행 방법 기재
- [ ] 배포 URL이 `https://`로 시작
- [ ] **시크릿 창(로그아웃 상태)** 에서 배포 URL 정상 실행
- [ ] 배포 URL에서 강원랜드 공공데이터가 정상 표시
- [ ] 로그인 없이 접속 가능 (Vercel Password Protection OFF 확인)
- [ ] **휴대폰에서 배포 URL 열림** 확인
- [ ] 제출 양식에 팀명·산출물명·한 줄 소개·활용 데이터셋명·저장소 URL·배포 URL 기재 (§3 문안)

프로젝트 특화 추가 항목:

- [ ] 커밋 이력 시크릿 스캔 통과 + `기획서_V.I.B.E.pdf` 미커밋 확인 (§4)
- [ ] `seed_demo.py --reset`으로 데모 초기 상태 복원 후 제출
- [ ] 대표 스크린샷 1장 캡처·제출 (허브 화면, §1)
- [ ] `git tag submission-final` + main 동결 (§5)
- [ ] 심사 기간 중 `sam delete` 금지 — AWS 철거(09 §6)는 **심사·전시 종료 후**로 연기
- [ ] Billing 알림 $1 활성 상태 확인

## 7. 타 문서 연동

- **10-timeline**: Phase 0에 "GitHub 초기 커밋", Phase 6 Gate에 "§6 체크리스트 전항 통과" 반영됨
- **09-deployment**: §5 심사 기간 운영 모드(콜드스타트·리셋·동시성)의 구현 항목 반영됨
- **13-design-guide**: §8 반응형 최소선이 "휴대폰에서 열림" 요건의 구현 기준
- **01-overview**: 성공 기준에 제출 요건 게이트 1건(§6 체크리스트 전항 통과) 추가됨
