[pytest]
testpaths = tests
# 통제 불가한 서드파티 경고만 억제 — 우리 코드의 경고는 그대로 보이게 둔다 (14 문서 T14 '깨끗한 출력').
# 네 번째 필드(발생 모듈)까지 지정해 mangum 밖에서 같은 경고가 나면 그대로 보이게 한다 —
# 모듈 제한 없이 걸면 우리 코드가 같은 경고를 내도 조용히 묻힌다.
filterwarnings =
    ignore:There is no current event loop:DeprecationWarning:mangum\.adapter
